{
  "name": "{{ project_name }}",
  "version": "0.0.0",
  "scripts": {
    "start": "cd app && pm2 start development.json"
  },
{% if remote_repo_url %}
  "repository": {
    "type": "git",
    "url": "{{ remote_repo_url }}"
  },
{% endif %}
  "dependencies": {},
  "devDependencies": {}
}
